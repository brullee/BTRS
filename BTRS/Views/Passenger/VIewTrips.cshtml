@model IEnumerable<BTRS.Models.Bus_busTrips>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Book trips</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Trip ID
            </th>
            <th>
                Destination
            </th>
            <th>
                Start Date
            </th>
            <th>
                End Date
            </th>
            <th>
                Captain name
            </th>
            <th>
                Number of seats
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.trip.TripId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.trip.Destination)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.trip.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.trip.EndDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.bus.CptName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.bus.NOofSeats)
                </td>
                <td>
                    <a asp-action="AddTrip" asp-route-id="@item.trip?.TripId">Book trip</a> |
                    <a asp-action="TripDetails" asp-route-id="@item.trip?.TripId">Details</a> |
                </td>
            </tr>
        }
    </tbody>
</table>
